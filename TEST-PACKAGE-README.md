# 赴港通 Demo 测试包

## 运行要求

- Node.js 20 或更高版本
- 可访问飞书开放平台的网络（只有测试飞书写入时需要）

## 启动

1. 将 `.env.example` 复制为 `.env`。
2. 填入飞书 App ID、App Secret；如需调试真实 AI，再填写模型接口配置。
3. 在当前目录执行：`npm start`
4. 浏览器访问：`http://127.0.0.1:4173/`

演示固定验证码：`888888`。

未配置 AI Key 时使用本地调试回复；未配置飞书时仍可体验页面和评估，但不会写入数据表。

## 已有功能

- 手机号与固定验证码登录
- 未登录状态的 AI 使用拦截
- 赴港评估及确定性结论
- 在港状态评估及确定性结论
- 评估结果、雷达图、行动路径
- AI 顾问调试入口
- 客户信息、赴港记录、在港记录写入飞书
- 账号称呼保存
- 首页实用信息、关于我们、商务合作及联系我们

## 测试注意

- 请使用专门的测试手机号，评估提交会写入配置的飞书正式表。
- App Secret 和 AI Key 只能放在服务端 `.env`，不要提交到前端或公开仓库。
- 当前为功能测试包，不建议直接作为生产服务部署。

### 用户状态隔离回归

- 首次打开新版本时，不应展示历史测试手机号、评估结果或问卷答案。
- 有本地令牌时，必须先通过 `/api/session` 校验，校验成功后才显示登录资料。
- 未登录进入两个评估入口时，答案对象必须为空；登录后草稿按用户 ID 和评估类型分别保存。
- 退出登录后，手机号、企业资料、评估结果、两类问卷草稿和聊天缓存均不得出现在未登录页面。
- 使用两个不同手机号依次登录，第二个账号不得看到第一个账号的本地资料或问卷草稿。

## Render 部署

- 服务类型：Web Service
- Build Command：`npm install`
- Start Command：`npm start`
- Health Check Path：`/api/status`
- 在 Render 的 Environment 页面配置 `.env.example` 中的变量，密钥不要提交到代码仓库。
- Render 会自动提供 `PORT`，应用已监听 `0.0.0.0`。
